export enum CSS_VAR {
  DIVIDER_COLOR = '--divider-color',
  DIVIDER_BG_COLOR = '--divider-bg-color',
  DIVIDER_BORDER_TOP_COLOR = '--divider-border-top-color',
  DIVIDER_BORDER_RADIUS = '--divider-border-radius',
  DIVIDER_MARGIN_RADIUS = '--divider-margin-radius',
  SCROLLBAR_THUMB_COLOR = '--scrollbar-thumb-color',
  SIDEBAR_BACKGROUND_COLOR = '--sidebar-background-color',
  SIDEBAR_TEXT_COLOR = '--sidebar-text-color',
  SIDEBAR_TEXT_TRANSFORM = '--sidebar-text-transform',
  SO_TOOLTIP_BACKGROUND_COLOR = '--so-tooltip-background-color',
  SO_TOOLTIP_TEXT_COLOR = '--so-tooltip-text-color',
  SO_BACKDROP_FILTER = '--so-backdrop-filter',
  CUSTOM_SIDEBAR_WIDTH = '--custom-sidebar-width',
}

export enum CSS_VAR_VALUE {
  PRIMARY_BACKGROUND_COLOR = 'var(--primary-background-color)',
  PRIMARY_TEXT_COLOR = 'var(--primary-text-color)',
  BLUR_10PX = 'blur(10px)',
  TRANSPARENT = 'transparent',
}
